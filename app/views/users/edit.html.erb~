<% provide(:title, "Edycja Użytkownika") %>
<h1>Zaktualizuj  profil</h1>

<div class="row">
  <div class="span6 offset3">
  <%= form_for(@user) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :Nazwa %>
      <%= f.text_field :name %>

      <%= f.label :"E-mail" %>
      <%= f.text_field :email %>

      <%= f.label :Hasło %>
      <%= f.password_field :password %>

      <%= f.label :password_confirmation, "Potwierdz hasło" %>
      <%= f.password_field :password_confirmation %>

      <%= f.submit "Zapisz", class: "btn btn-large btn-primary" %>
    <% end %>

    <!--  <%= gravatar_for @user %>
    <a href="http://gravatar.com/emails">change</a> --!> 
  </div>
</div>
